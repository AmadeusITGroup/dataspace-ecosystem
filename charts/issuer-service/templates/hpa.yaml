{{- if .Values.issuerservice.autoscaling.enabled }}
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dse.fullname" . }}
  labels:
    {{- include "dse.dse.issuerservice.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dse.fullname" . }}
  minReplicas: {{ .Values.issuerservice.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.issuerservice.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.issuerservice.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.issuerservice.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.issuerservice.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.issuerservice.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
