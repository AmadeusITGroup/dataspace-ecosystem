apiVersion: v1
kind: Service
metadata:
  {{- with .Values.issuerservice.service.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
  {{- end }}
  name: {{ include "dse.fullname" . }}
  namespace: {{ .Release.Namespace | default "default" | quote }}
  labels:
    {{- include "dse.labels" . | nindent 4 }}
    {{- with .Values.issuerservice.service.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.issuerservice.service.type }}
  ports:
    {{- range $key,$value := .Values.issuerservice.endpoints }}
    - name: {{ $key }}
      targetPort: {{ $key }}
      port: {{ $value.port }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "dse.issuerservice.selectorLabels" . | nindent 4 }}
