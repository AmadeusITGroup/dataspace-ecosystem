apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dse.fullname" . }}-config
  labels:
    {{- include "dse.labels" . | nindent 4 }}
data:

  logging.properties: |-
    {{- .Values.federatedcatalogfilter.logging | nindent 4 }}

  opentelemetry.properties: |-
    {{- .Values.federatedcatalogfilter.opentelemetry | nindent 4 }}

  config.properties: |-
    {{- .Values.federatedcatalogfilter.config | nindent 4 }}