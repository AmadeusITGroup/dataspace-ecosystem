apiVersion: v1
kind: Service
metadata:
  {{- with .Values.federatedcatalogfilter.service.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
  {{- end }}
  name: {{ include "dse.fullname" . }}
  namespace: {{ .Release.Namespace | default "default" | quote }}
  labels:
    {{- include "dse.labels" . | nindent 4 }}
    {{- with .Values.federatedcatalogfilter.service.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.federatedcatalogfilter.service.type }}
  ports:
    {{- range $key,$value := .Values.federatedcatalogfilter.endpoints }}
    - name: {{ $key }}
      targetPort: {{ $key }}
      port: {{ $value.port }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "dse.federatedcatalogfilter.selectorLabels" . | nindent 4 }}
