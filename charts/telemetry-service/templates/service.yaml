apiVersion: v1
kind: Service
metadata:
  {{- with .Values.telemetryservice.service.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
  {{- end }}
  name: {{ include "dse.fullname" . }}
  namespace: {{ .Release.Namespace | default "default" | quote }}
  labels:
    {{- include "dse.labels" . | nindent 4 }}
    {{- with .Values.telemetryservice.service.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.telemetryservice.service.type }}
  ports:
    {{- range $key,$value := .Values.telemetryservice.endpoints }}
    - name: {{ $key }}
      targetPort: {{ $key }}
      port: {{ $value.port }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "dse.telemetryservice.selectorLabels" . | nindent 4 }}
