apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dse.fullname" . }}-config
  labels:
    {{- include "dse.labels" . | nindent 4 }}
data:
  logging.properties: |-
    {{- .Values.telemetryservice.logging | nindent 4 }}

  opentelemetry.properties: |-
    {{- .Values.telemetryservice.opentelemetry | nindent 4 }}

  config.properties: |-
    {{- .Values.telemetryservice.config | nindent 4 }}