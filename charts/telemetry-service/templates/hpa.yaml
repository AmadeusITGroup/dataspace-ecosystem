{{- if .Values.telemetryservice.autoscaling.enabled }}
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dse.fullname" . }}
  labels:
    {{- include "dse.dse.telemetryservice.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dse.fullname" . }}
  minReplicas: {{ .Values.telemetryservice.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.telemetryservice.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.telemetryservice.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.telemetryservice.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.telemetryservice.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.telemetryservice.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
