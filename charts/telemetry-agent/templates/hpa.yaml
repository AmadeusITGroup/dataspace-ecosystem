{{- if .Values.telemetryagent.autoscaling.enabled }}
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dse.fullname" . }}
  labels:
    {{- include "dse.dse.telemetryagent.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dse.fullname" . }}
  minReplicas: {{ .Values.telemetryagent.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.telemetryagent.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.telemetryagent.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.telemetryagent.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.telemetryagent.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.telemetryagent.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
