{{- if .Values.telemetrystorage.autoscaling.enabled }}
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dse.fullname" . }}
  labels:
    {{- include "dse.dse.telemetrystorage.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dse.fullname" . }}
  minReplicas: {{ .Values.telemetrystorage.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.telemetrystorage.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.telemetrystorage.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.telemetrystorage.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.telemetrystorage.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.telemetrystorage.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
