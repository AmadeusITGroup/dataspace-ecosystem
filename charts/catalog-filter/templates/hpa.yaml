{{- if .Values.federatedcatalogfilter.autoscaling.enabled }}
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dse.fullname" . }}
  labels:
    {{- include "dse.dse.federatedcatalogfilter.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dse.fullname" . }}
  minReplicas: {{ .Values.federatedcatalogfilter.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.federatedcatalogfilter.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.federatedcatalogfilter.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.federatedcatalogfilter.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.federatedcatalogfilter.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.federatedcatalogfilter.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
