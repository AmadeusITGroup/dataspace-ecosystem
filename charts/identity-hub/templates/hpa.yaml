{{- if .Values.identityhub.autoscaling.enabled }}
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dse.fullname" . }}
  labels:
    {{- include "dse.dse.identityhub.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dse.fullname" . }}
  minReplicas: {{ .Values.identityhub.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.identityhub.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.identityhub.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.identityhub.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.identityhub.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.identityhub.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
