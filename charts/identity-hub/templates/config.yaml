apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dse.fullname" . }}-config
  labels:
    {{- include "dse.labels" . | nindent 4 }}
data:

  logging.properties: |-
    {{- .Values.identityhub.logging | nindent 4 }}

  opentelemetry.properties: |-
    {{- .Values.identityhub.opentelemetry | nindent 4 }}

  config.properties: |-
    {{- .Values.identityhub.config | nindent 4 }}