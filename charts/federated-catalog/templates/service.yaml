apiVersion: v1
kind: Service
metadata:
  {{- with .Values.federatedcatalog.service.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
  {{- end }}
  name: {{ include "dse.fullname" . }}
  namespace: {{ .Release.Namespace | default "default" | quote }}
  labels:
    {{- include "dse.labels" . | nindent 4 }}
    {{- with .Values.federatedcatalog.service.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.federatedcatalog.service.type }}
  ports:
    {{- range $key,$value := .Values.federatedcatalog.endpoints }}
    - name: {{ $key }}
      targetPort: {{ $key }}
      port: {{ $value.port }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "dse.federatedcatalog.selectorLabels" . | nindent 4 }}
