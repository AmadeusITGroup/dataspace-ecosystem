{{- if .Values.federatedcatalog.autoscaling.enabled }}
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dse.fullname" . }}
  labels:
    {{- include "dse.dse.federatedcatalog.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dse.fullname" . }}
  minReplicas: {{ .Values.federatedcatalog.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.federatedcatalog.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.federatedcatalog.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.federatedcatalog.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.federatedcatalog.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.federatedcatalog.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
