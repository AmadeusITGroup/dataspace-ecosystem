apiVersion: v1
kind: Service
metadata:
  {{- with .Values.telemetrycsvmanager.service.annotations }}
  annotations:
  {{- toYaml . | nindent 4 }}
  {{- end }}
  name: {{ include "dse.fullname" . }}
  namespace: {{ .Release.Namespace | default "default" | quote }}
  labels:
    {{- include "dse.labels" . | nindent 4 }}
    {{- with .Values.telemetrycsvmanager.service.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.telemetrycsvmanager.service.type }}
  ports:
    {{- range $key,$value := .Values.telemetrycsvmanager.endpoints }}
    - name: {{ $key }}
      targetPort: {{ $key }}
      port: {{ $value.port }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "dse.telemetrycsvmanager.selectorLabels" . | nindent 4 }}
