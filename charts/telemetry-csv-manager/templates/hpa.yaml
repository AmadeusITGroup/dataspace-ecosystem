{{- if .Values.telemetrycsvmanager.autoscaling.enabled }}
---
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dse.fullname" . }}
  labels:
    {{- include "dse.dse.telemetrycsvmanager.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dse.fullname" . }}
  minReplicas: {{ .Values.telemetrycsvmanager.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.telemetrycsvmanager.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.telemetrycsvmanager.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.telemetrycsvmanager.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.telemetrycsvmanager.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.telemetrycsvmanager.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
