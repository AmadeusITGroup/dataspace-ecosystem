apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dse.fullname" . }}-config
  labels:
    {{- include "dse.labels" . | nindent 4 }}
data:
  logging.properties: |-
    {{- .Values.telemetrycsvmanager.logging | nindent 4 }}

  opentelemetry.properties: |-
    {{- .Values.telemetrycsvmanager.opentelemetry | nindent 4 }}

  config.properties: |-
    {{- .Values.telemetrycsvmanager.config | nindent 4 }}